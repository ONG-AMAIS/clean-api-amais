version: '3.9'

services:
  app:
    build: .
    ports:
      - 80:5000

  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    ports:
      - 3306:3306
    env_file:
      - .env
      


      